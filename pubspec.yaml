name: puja_path
description: A new Flutter application.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.2.0
  carousel_slider: ^4.0.0
  cupertino_icons: ^1.0.0
  device_info: ^2.0.3
  dio: ^4.0.4
  get: ^4.3.8
  intl: ^0.17.0
  flutter_rating_bar: ^4.0.0
  flutter_screenutil: ^5.0.0+2
  flutter_svg: ^1.0.0
  fluttertoast: ^8.0.8
  nepali_date_picker: ^5.2.0
  shared_preferences: ^2.0.9
  sms_autofill: ^2.2.0
  url_launcher: ^6.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
